import { AbstractDevice } from "./AbstractDevice";
import { IEcrDevice } from "./IEcrDevice";
import { IProg } from "./interfaces/IProg";
/**
 * ECR Devoce
 * @inheritdoc
 *
 * @export
 * @class EcrDevice
 * @typedef {EcrDevice}
 * @extends {AbstractDevice}
 * @implements {IEcrDevice}
 */
export declare class EcrDevice extends AbstractDevice implements IEcrDevice {
    /**
     * @inheritdoc
     */
    fwVersion: string;
    /**
     * @inheritdoc
     */
    fwVersionLabel: string;
    /**
     * @inheritdoc
     */
    hasProgDump: boolean;
    /**
     * @inheritdoc
     */
    nDepartments: number;
    /**
     * @inheritdoc
     */
    nOperators: number;
    /**
     * @inheritdoc
     */
    nPayments: number;
    /**
     * @inheritdoc
     */
    nVats: number;
    /**
     * @inheritdoc
     */
    prog: IProg | null;
    /**
     * @inheritdoc
     */
    hasDgfeFreeSpace: boolean;
}
