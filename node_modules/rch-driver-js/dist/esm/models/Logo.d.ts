import { ILogo } from "../interfaces/ILogo";
/**
 * @inheritdoc
 *
 * @export
 * @class Logo
 * @typedef {Logo}
 * @implements {ILogo}
 */
export declare class Logo implements ILogo {
    /**
     * @inheritdoc
     */
    id: number;
    /**
     * @inheritdoc
     */
    value: number;
}
