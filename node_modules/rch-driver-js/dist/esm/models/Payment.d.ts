import { IPayment } from "../interfaces/IPayment";
import { AbstractParser } from "./AbstractParser";
/**
 * @inheritdoc
 *
 * @export
 * @class Payment
 * @typedef {Payment}
 * @extends {AbstractParser}
 * @implements {IPayment}
 */
export declare class Payment extends AbstractParser implements IPayment {
    /**
     * @inheritdoc
     */
    id: number;
    /**
     * @inheritdoc
     */
    name: string;
    /**
     * @inheritdoc
     */
    change: boolean;
    /**
     * @inheritdoc
     */
    cash: boolean;
    /**
     * @inheritdoc
     */
    credit: boolean;
    /**
     * @inheritdoc
     */
    drawer: boolean;
    /**
     * @inheritdoc
     */
    ticket: boolean;
    /**
     * @inheritdoc
     */
    inputTotalAmount: boolean;
    /**
     * @inheritdoc
     */
    payDiscount: boolean;
    /**
     * @inheritdoc
     */
    creditType: number;
    /**
     * Creates an instance of Payment.
     * @date 1/11/2022 - 12:15:32 PM
     *
     * @constructor
     * @param {string} entry
     */
    constructor(entry: string);
}
