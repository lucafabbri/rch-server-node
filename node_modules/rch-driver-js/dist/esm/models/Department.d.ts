import { AbstractParser } from "./AbstractParser";
import { IDepartment } from "../interfaces/IDepartment";
/**
 * @inheritdoc
 *
 * @export
 * @class Department
 * @typedef {Department}
 * @extends {AbstractParser}
 * @implements {IDepartment}
 */
export declare class Department extends AbstractParser implements IDepartment {
    /**
     * @inheritdoc
     */
    firstRow: string;
    /**
     * @inheritdoc
     */
    secondRow: string;
    /**
     * @inheritdoc
     */
    id: number;
    /**
     * @inheritdoc
     */
    name: string;
    /**
     * @inheritdoc
     */
    departmentType: number;
    /**
     * @inheritdoc
     */
    price: number;
    /**
     * @inheritdoc
     */
    halo: number;
    /**
     * @inheritdoc
     */
    lalo: number;
    /**
     * @inheritdoc
     */
    single: boolean;
    /**
     * @inheritdoc
     */
    vatCode: number;
    /**
     * @inheritdoc
     */
    groupCode: number;
    /**
     * Creates an instance of Department.
     * @date 1/11/2022 - 12:12:44 PM
     *
     * @constructor
     * @param {string} firstRow
     * @param {string} secondRow
     */
    constructor(firstRow: string, secondRow: string);
}
